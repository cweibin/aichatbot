server:
  port: 6501
  servlet:
    context-path: /api
spring:
  profiles:
    active: dev
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    druid:
      validation-query: SELECT 1
      web-stat-filter:
        enabled: true
      stat-view-servlet:
        # http://localhost:8800/druid/ admin/admin
        enabled: true
        login-username: admin
        login-password: admin
  mvc:
    static-path-pattern: /files/**
  resources:
    static-locations: file:/tmp/aezo-chat-gpt/data
  mail:
    host: smtp.exmail.qq.com
    port: 465
    username: test@example.com
    password: test
    properties:
      from: ${aezo-app-common.project-name}<${spring.mail.username}>
      mail.smtp.socketFactory.class: javax.net.ssl.SSLSocketFactory

sq-app-common:
  enc-pass-key: "aezo-chat-gpt"

sq-mini-tools:
  miniapp:
    configs:
      - sqAppCode: sq-mini-tools
        # 填写你的小程序id和秘钥
        appid: xxxxxxxxx
        secret: xxxxxxxxx
        token: # 微信小程序消息服务器配置的token
        aesKey: # 微信小程序消息服务器配置的EncodingAESKey
        msgDataFormat: JSON
  openai:
    enable: false
    # 可换成自己的 OpenAI 兼容地址，例如 https://code.rayinai.com/
    api-host: https://code.rayinai.com/
    # 填写自己的 OpenAI key
    api-key: sk-262ec55895972f1ce5a577178a35fb717a3f29a89365ae2093560ee3e2f442a0
    # 阻塞式聊天默认走 /v1/chat/completions
    chat-model: gpt-5.4
  chat:
    regist-num: 10
    daily-free-num: 5
    invite-num: 10
    invite-max: 5
    ads-num: 3
    ads-max: 10

aezo-app-common:
  project-name: One能聊天
  project-code: aezo-chat-gpt
  enc-pass-key: aezo-chat-gpt
  # 管理员邮箱，用于接收一些系统预警信息
  mgrEmail:
  # 短信配置
  sms:
    # 激活哪种短信发送方式: aliyun|tencent
    active: tencent
    aliyun:
      access-key-id: xxxxxxxxx
      access-key-secret: xxxxxxxxx
      sign-name: xxxxxxxxx
      template-code: SMS_xxxxxxxxx
      template-param: code
    tencent:
      access-key-id: xxxxxxxxx
      access-key-secret: xxxxxxxxx
      app-id: xxxxxxxxx
      sign-name: xxxxxxxxx
      template-code: xxxxxxxxx
  # 支付配置
  pay:
    goodsPrice:
      "1": "1.99,NumBag,20"
      "2": "4.90,DayBag,300"
      "3": "19.90,MonthBag,300"
      "4": "99.90,YearBag,300"
    payInvokeType: epay
    orderTimeout: 10
    ekey:
      gid: 7
      ekey_label_text_logo: One能聊天
      ekey_label_email: 用户名
      search_pwd: 123
      manager_email:
    epay:
      url: https://example.com/api/create-order
      pid: OC_DEMO
      key: 123
      notify_url: http://localhost:6501/api/tools/order/ekeyPaidApiHook
      return_url: http://localhost:8080/

aezo-chat-gpt:
  thirdapp:
    configs:
      - sqAppCode: aezo-chat-gpt
        sqAppType: WxMa
        appid: xxxxxxxxx
        secret: xxxxxxxxx
        token:
        aesKey:
        msgDataFormat: JSON
      - sqAppCode: aezo-smallelife
        sqAppType: WxMp
        appid: xxxxxxxxx
        secret: xxxxxxxxx
        config-storage:
          http-client-type: HttpClient
  chat:
    regist-num: 5
    daily-free-num: 1
    invite-num: 3
    invite-max: 10
    ads-num: 2
    ads-max: 5
    regist-num-m2: 1

mybatis-plus:
  mapper-locations: classpath*:/mapper/*.xml,classpath*:/mapper/**/*.xml
  global-config:
    db-config:
      # 逻辑删除字段. IService#remove则是修改逻辑字段(原本为硬删除)，IService的方法则全部加valid_status=1，但是手写的Mapper则需要手动加此条件
      logic-delete-field: valid_status
      # 逻辑已删除值
      logic-delete-value: 0
      # 逻辑未删除值
      logic-not-delete-value: 1

logging:
  config: classpath:logback-spring.xml

############## report-table 配置 ##############
report-table:
  baseFolder: ${spring.resources.static-locations}
  adminReportPassword: F1Sa%cj*yx2deV3K
  roleKey:
    prefix:
    manager: MGR1
  core:
    jobModUrl: http://localhost:${server.port}
  job:
    coreModUrl: http://localhost:${server.port}

############## Sa-Token 配置 (文档: https://sa-token.cc) ##############
sa-token:
  # token名称 (同时也是cookie名称)
  token-name: SQ-ACCESS-TOKEN
  # token有效期，单位s 默认12小时, -1代表永不过期
  timeout: 43200
  # token临时有效期 (指定时间内无操作就视为token过期) 单位: 秒
  activity-timeout: 3600
  # 是否允许同一账号并发登录 (为true时允许一起登录, 为false时新登录挤掉旧登录)
  is-concurrent: true
  # 在多人登录同一账号时，是否共用一个token (为true时所有登录共用一个token, 为false时每次登录新建一个token)
  is-share: true
  # token风格
  token-style: uuid
  # 是否输出操作日志
  is-log: false
